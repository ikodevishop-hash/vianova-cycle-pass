# dependencies
**/node_modules/

# server data (SQLite DB, JWT secret) — never commit
vianova_server/data/

# Expo / build outputs
vianova_app/.expo/
**/dist/
vianova_app/expo-env.d.ts

# env / secrets
**/.env
**/.env*.local

# misc
.DS_Store
*.log
# stray empty file created by pasting "… -> main" into cmd (> main redirect)
/main
